import { Fragment } from 'react';
import LeadList from '../../component/lead/lead-list';

const Lead = () => {
    return (
        <Fragment>
            <LeadList />
        </Fragment>
    );
};

export default Lead;